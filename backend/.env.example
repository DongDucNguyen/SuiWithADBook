# SUI Backend Configuration
# ⚠️ QUAN TRỌNG: KHÔNG commit file này lên git!
# Thêm .env vào .gitignore

# Admin wallet private key (dùng để gửi reward cho users)
# Để lấy private key từ Slush/Sui Wallet:
# 1. Mở Slush Wallet extension
# 2. Vào Settings > Export Private Key
# 3. Copy và paste vào đây
ADMIN_PRIVATE_KEY=your_private_key_here

# Network: testnet, mainnet, devnet
SUI_NETWORK=testnet

# Số SUI thưởng cho mỗi quiz hoàn thành 100%
REWARD_AMOUNT_SUI=0.1

# Backend port
PORT=3001
